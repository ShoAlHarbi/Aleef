import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { StyleSheet, Text, View, TextInput, TouchableOpacity } from 'react-native';

class signUp {
    render() {
        return (
            <View style={styles.container}>
                <Text>تسجيل حساب جديد</Text>
                <TextInput style={styles.textInput} placeholder="الاسم"></TextInput>
                <TextInput style={styles.textInput} placeholder="اسم المستخدم"></TextInput>
                <TextInput style={styles.textInput} placeholder="البريد الالكتروني"></TextInput>
                <TextInput style={styles.textInput} placeholder="كلمة المرور"></TextInput>
                <TextInput style={styles.textInput} placeholder="تأكيد كلمة المرور"></TextInput>
                <TouchableOpacity>
                    <Text>سجل معنا</Text>
                </TouchableOpacity>

                <StatusBar style="auto" />
            </View>
        );
    }
}


const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    textInput: {
        fontSize: 18,
        color: "#fff",
    }
});
